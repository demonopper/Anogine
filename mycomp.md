{{
  -- context switch operator its lua mode
  {{its text mode}}
}}

${} -- expression operator can only used in text mode
${{}} -- expression buffer operator it doesnt output into global buffer and returns a statement can only be used in lua mode